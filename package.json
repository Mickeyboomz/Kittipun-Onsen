{
  "name": "kittipun-onsen",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --outfile=index.js --format=iife --platform=browser",
    "dev": "esbuild src/main.js --bundle --outfile=index.js --format=iife --platform=browser --watch",
    "vercel-build": "echo Skip build"
  },
  "dependencies": {
    "firebase": "^11.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
